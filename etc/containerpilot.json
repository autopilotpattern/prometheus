{
  "consul": "{{ .CONSUL }}:8500",
  "preStart": "prestart.sh",
  "services": [
    {
      "name": "prometheus",
      "port": 9090,
      "health": ["curl", "-so", "/dev/null", "http://localhost:9090/metrics"],
      "poll": 10,
      "ttl": 25
    }
  ]
}
